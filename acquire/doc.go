// Package acquire composes direct OBI retrieval, optional HTTP Discovery, and
// caller-supplied interface synthesis. Each underlying capability remains
// independently usable; the resolution order is this package's convenience
// policy, not a Core or HTTP Discovery requirement.
package acquire
